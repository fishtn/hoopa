# -*- coding: utf-8 -*-

from hoopa import Spider
from hoopa import Item
from hoopa import Middleware


class DataItem(Item):
    pass


class ${spider_class_name}(Spider):
    name = "${spider_name}"

    def start_requests(self):
        pass

    def parse(self, request, response):
        print(response)

    def process_item(self, item_list: list):
        for item in item_list:
            print(item)


middleware = Middleware()


@middleware.request
def set_request_middleware(spider_ins, request):
    pass


@middleware.response
def set_response_middleware(spider_ins, request, response):
    pass


if __name__ == "__main__":
    middleware_list = [middleware]
    ${spider_class_name}.start(middleware=middleware_list)